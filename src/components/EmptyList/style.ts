import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  emptyContainer: {
    marginTop: 48,
    alignItems: 'center',
    justifyContent: 'center',
  },

  textOne: {
    color: '#808080',
    fontSize: 14,
    fontWeight: 'bold',
  },
  
  textSecond: {
    color: '#808080',
    fontSize: 14,
  },

  climpboard: {
    marginBottom: 20,
  }
})